---
const { url, name } = Astro.props;
---

<div
  class="flex cursor-pointer flex-col items-center gap-2 rounded-2xl border border-[var(--border-color)] bg-white/5 p-4 transition duration-200 hover:-translate-y-[3px] hover:border-[var(--accent)]"
>
  <img class="h-12 w-12 object-contain" src={`${url}`} alt={`imagen_logo_${name}`} loading="lazy" />
  <p class="text-sm font-semibold text-[var(--text-primary)]">{name}</p>
</div>
