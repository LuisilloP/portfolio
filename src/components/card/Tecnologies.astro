---
const { url, name } = Astro.props;
---

<div
  class="flex flex-col hover:scale-105 ease-in-out duration-200 cursor-pointer items-center"
>
  <img class="w-[70px] h-[100%]" src={`${url}`} alt={`imagen_logo_${name}`} />
  <p>{name}</p>
</div>
